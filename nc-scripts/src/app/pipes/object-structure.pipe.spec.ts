import { ObjectStructurePipe } from './object-structure.pipe';

describe('ObjectStructurePipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectStructurePipe();
    expect(pipe).toBeTruthy();
  });
});
